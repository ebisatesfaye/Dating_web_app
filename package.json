{
  "name": "whaatachi",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\" --names \"API,WEB\" --prefix-colors \"magenta,cyan\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "seed": "npm run db:seed --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
